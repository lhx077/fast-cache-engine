@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(NOT WIN32)
  find_dependency(Threads)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/fast_cache_engineTargets.cmake")

check_required_components(fast_cache_engine)
